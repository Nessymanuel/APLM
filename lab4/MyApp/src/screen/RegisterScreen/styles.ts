// src/components/RegisterScreen/styles.js
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 20,
    },
    input: {
        marginBottom: 10,
        borderBottomWidth: 1,
        borderColor: 'gray',
        padding: 10,
    },
});

export default styles;
