// src/components/LoginScreen/styles.js
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems:'center',
        padding: 20,
    },
    input: {
        marginBottom: 10,
        borderBottomWidth: 1,
        borderColor: 'gray',
        padding: 10,
    },
});

export default styles;
